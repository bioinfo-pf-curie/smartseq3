name: SmartSeq3
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - conda-forge::python=3.6.10
  - conda-forge::numpy
  - bioconda::rseqc
  - bioconda::deeptools
  - bioconda::multiqc
  - bioconda::cutadapt
  - conda-forge::r-base=3.6.3
  - conda-forge::r-seurat

  => Found::
    seurat=3.2.2 => ne marche pas 
    library(Seurat)
      Error: package or namespace load failed for ‘Seurat’ in dyn.load(file, DLLpath = DLLpath, ...):
      unable to load shared object '/bioinfo/users/lhadjabe/R/x86_64-pc-linux-gnu-library/4.0/KernSmooth/libs/KernSmooth.so':
        libRblas.so: cannot open shared object file: No such file or directory

    rseqc=4.0.0
    numpy=1.19.4
    deepltools=3.5.0